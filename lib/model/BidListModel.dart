class BidListModel {
  final String points;
  final String digit;
  final String pana;
  final String session;
  final String mType;
  BidListModel(this.digit, this.points, this.pana, this.session, this.mType);
}
